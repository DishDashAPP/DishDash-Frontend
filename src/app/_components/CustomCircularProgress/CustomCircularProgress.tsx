'use client'

import {FC} from "react";
import CircularProgress from "@mui/material/CircularProgress";

const CustomCircularProgress: FC = () => {
    return (
        <CircularProgress className={'mx-auto mt-20'}/>
    )
}

export default CustomCircularProgress